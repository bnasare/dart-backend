name: dart_backend_tech_test
description: Production-ready notes service backend built with Shelf and clean architecture.
version: 0.1.0

environment:
  sdk: ^3.4.0

dependencies:
  drift: ^2.18.0
  sqlite3: ^2.4.7
  path: ^1.9.0
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  shelf_static: ^1.1.2
  http_parser: ^4.0.2
  logging: ^1.2.0
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.4.10
  drift_dev: ^2.18.0
  test: ^1.24.9
  lints: ^3.0.0
